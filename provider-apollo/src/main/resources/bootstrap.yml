server:
  port: 8081
spring:
  application:
    name: provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
app:
  id: do1cloud-apaas
apollo:
  bootstrap:
    enabled: true
    namespaces: local
    eagerLoad:
      enabled: true
  meta: http://134.175.141.130:8231

management:
  endpoints:
    web:
      exposure:
        include: "*"
